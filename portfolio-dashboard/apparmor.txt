#include <tunables/global>

profile portfolio_dashboard flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  network,
  capability net_bind_service,
  capability setuid,
  capability setgid,
  capability dac_override,
  capability dac_read_search,
  capability chown,
  capability fowner,
  capability kill,
  capability sys_chroot,

  signal,

  /init ix,
  /bin/** ix,
  /sbin/** ix,
  /usr/bin/** ix,
  /usr/sbin/** ix,
  /usr/local/bin/** ix,
  /lib/** mr,
  /usr/lib/** mr,
  /usr/local/lib/** mr,
  /etc/ld.so.cache r,
  /etc/ld.so.conf r,
  /etc/ld.so.conf.d/{,**} r,
  /etc/passwd r,
  /etc/group r,
  /etc/nsswitch.conf r,
  /etc/resolv.conf r,
  /etc/hosts r,
  /etc/ssl/{,**} r,
  /etc/timezone r,
  /etc/localtime r,
  /usr/share/zoneinfo/** r,
  /etc/services r,

  /run/{s6,s6-rc*,service}/** rwix,
  /package/** rwix,
  /command/** rwix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /etc/s6-overlay/** rwix,

  /usr/bin/node rix,
  /usr/sbin/nginx rix,

  /app/** r,
  /app/backend/node_modules/** r,
  /data/** rw,
  /run/** rwk,
  /tmp/** rwk,
  /var/log/nginx/** rw,
  /var/lib/nginx/** rw,
  /etc/nginx/** r,
  /run/nginx.pid rw,

  /proc/sys/kernel/random/uuid r,
  /proc/sys/net/core/somaxconn r,
  /proc/*/stat r,
  /proc/*/status r,
  /proc/meminfo r,
  /proc/cpuinfo r,
  /proc/loadavg r,
  /proc/uptime r,

  deny /proc/sys/** wklx,
  deny /sys/** wklx,
  deny /boot/** rwklx,
  deny /lib/modules/** rwklx,
  deny mount,
  deny ptrace,
}
